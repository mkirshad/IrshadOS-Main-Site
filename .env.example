# Used for canonical URLs + sitemap/robots output.
SITE_URL=https://irshados.com

# Optional: runtime API source for apps/ebooks (falls back to local content if unavailable).
IRSHADOS_API_BASE_URL=https://api.irshados.com

